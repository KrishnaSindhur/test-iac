# Use this ONLY when you have a working kubeconfig / cluster (kind, EKS, etc.).
# Rename to main.tf and remove terraform_data main.tf — apply will fail without a cluster.
#
# terraform {
#   required_providers {
#     kubectl = {
#       source  = "gavinbunney/kubectl"
#       version = ">= 1.7.0"
#     }
#   }
# }
#
# provider "kubectl" {}
#
# resource "kubectl_manifest" "demo" {
#   yaml_body = file("./manifest-v1.yaml")
# }
